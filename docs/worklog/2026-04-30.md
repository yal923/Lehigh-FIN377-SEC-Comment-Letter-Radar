# Worklog - 2026-04-30

## Final Results Tab Cleanup

- Removed the redundant `Result Tables` tab from `pages/02_Final_Results.py`.
- Kept the underlying result files in `outputs/tables/` unchanged so they remain available for audit and reproducibility.
- Verified syntax with `./.venv/bin/python -m py_compile pages/02_Final_Results.py`.

## Regression Explanation Notes

- Added a collapsible `Why Regression Matters` section at the bottom of the Final Results regression tab.
- The note explains the difference between event study, topic CAR, and regression, and frames regression as a cross-sectional test of whether `topic` and `severity_score` explain CAR differences.
- Verified syntax with `./.venv/bin/python -m py_compile pages/02_Final_Results.py`.

## Backend Regression Suite

- Updated `code/07_event_study_regression.py` to generate the official 12-model regression suite: 3 CAR windows by 4 control specifications.
- Switched the formal topic parameterization to `0 + C(topic)` so all six topic groups have displayed coefficients rather than one omitted baseline topic.
- Added three backend output tables: `outputs/tables/regression_coefficients_long.csv`, `outputs/tables/regression_model_summary.csv`, and `outputs/tables/regression_summary_table.csv`.
- Added a highlighted note at the top of the Final Results regression tab explaining that topic coefficients are topic-specific conditional CAR levels, not differences from an omitted baseline.
- Rebuilt Step 7 with `./.venv/bin/python run.py --step event_study`; the run produced 12 complete regression models with 273 observations each.
- Verified syntax with `./.venv/bin/python -m py_compile code/07_event_study_regression.py pages/02_Final_Results.py`.

## Regression Presentation Layout

- Reworked the Final Results regression tab to read the backend 12-model regression suite instead of running checkbox-driven regressions live in Streamlit.
- Added a compact regression design table explaining outcomes, main variables, controls, topic coding, and robust standard errors.
- Split the main coefficient summary into CAR-window tabs, with four model specifications per tab.
- Added model diagnostics under each CAR-window table and moved full coefficient tables, formulas, backend summary tables, and regression-ready input rows into expanders.
- Removed the redundant `Backend Regression Summary Table` expander from the Final Results regression page while keeping the backend CSV available for audit.
- Verified syntax with `./.venv/bin/python -m py_compile pages/02_Final_Results.py`.

## Final Results Conclusions

- Added two numeric conclusions to the Topic CAR tab, tying the descriptive event-study results to H1, H2, and H3.
- Added two numeric conclusions to the Regression Playground tab, tying the 12-model regression suite to H3 and H4.
- Current result framing: immediate CAR is mixed across topics, post-event drift varies by topic, severity is not significant in the 12-model suite, and topic effects provide partial exploratory but not uniformly robust evidence.
- Rewrote both conclusion sections into concise bullet-style presentation text.
- Moved both conclusion sections to the bottom of their respective Final Results tabs.
- Verified syntax with `./.venv/bin/python -m py_compile pages/02_Final_Results.py`.
