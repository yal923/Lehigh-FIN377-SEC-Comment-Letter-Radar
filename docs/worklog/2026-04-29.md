# 2026-04-29

## Official Structure and Workflow Step 1

- Created official root structure: `code/`, `data/`, `outputs/`, `pages/`, and `docs/`.
- Added `run.py` as the root pipeline orchestrator.
- Added `code/01_sp500_reference.py` as the first numbered official module.
- Added Streamlit entrypoint `app.py` and pages for workflow, final results, and research design.
- Implemented Step 1 workflow artifacts:
  - `data/raw/reference/sp500_constituents.csv`
  - `data/raw/reference/sp500_constituents.parquet`
  - `outputs/workflow/01_sp500_reference_summary.json`
  - `outputs/workflow/01_sp500_reference_preview.csv`
  - `outputs/tables/sp500_sector_summary.csv`
- Verified `run.py --step sp500` using live Wikipedia access after sandboxed DNS failed and the command was rerun with approved network access.

## Presentation Structure Refinement

- Updated the Streamlit structure to follow the instructor reference style: sidebar pages plus horizontal method tabs.
- Replaced the workflow page with `pages/01_Background_and_Methods.py`.
- Recorded the data-format policy: CSV/JSON are for human validation; Parquet is retained as the efficient pandas/Streamlit format when paired with readable outputs.
- Updated Step 1 display so the full table loads from Parquet when available, while preview and metadata remain CSV/JSON-backed.

## SEC Raw Cache Inventory

- Added `configs/sample_100_10y.json` from the validated prototype sample config.
- Added `code/02_sec_raw_inventory.py`.
- Updated `run.py` with `--step sec_raw`.
- Copied the 100-company 10-year SEC raw cache from `Archive Prototype/data/raw/sec/` into official `data/raw/sec/`.
- Generated Step 2 workflow artifacts:
  - `outputs/workflow/02_sec_raw_summary.json`
  - `outputs/workflow/02_sec_raw_preview.csv`
  - `outputs/workflow/02_sec_thread_inventory.csv`
  - `outputs/tables/sec_raw_form_summary.csv`
- Filled the Background & Methods SEC Raw Download tab with real preview and summary data.
- Recorded decisions that SEC raw data is shared across samples and that the future full S&P 500 run is an optional extension, not a replacement for the 100-company baseline.

## Filing to Thread Dataset

- Added `code/03_thread_dataset.py`.
- Updated `run.py` with `--step threads`.
- Built official processed datasets from the SEC raw extraction manifests:
  - `data/processed/thread_filing_manifest.csv`
  - `data/processed/thread_filing_manifest.parquet`
  - `data/processed/thread_level.csv`
  - `data/processed/thread_level.parquet`
- Generated Step 3 workflow artifacts:
  - `outputs/workflow/03_thread_dataset_summary.json`
  - `outputs/workflow/03_thread_preview.csv`
  - `outputs/workflow/03_thread_filing_preview.csv`
  - `outputs/tables/thread_role_summary.csv`
- Filled the Background & Methods Filing to Thread tab with real preview and summary data.
- Run summary: 93 tickers with threads, 283 threads, 1,185 filing rows, 676 staff-letter rows, 504 filer-response rows, and 5 staff-action rows.

## Thread to Event Dataset

- Added `code/04_event_dataset.py`.
- Updated `run.py` with `--step events`.
- Built official event datasets from thread-level data:
  - `data/processed/event_level_raw.csv`
  - `data/processed/event_level_raw.parquet`
  - `data/processed/event_level_base.csv`
  - `data/processed/event_level_base.parquet`
- Generated Step 4 workflow artifacts:
  - `outputs/workflow/04_event_dataset_summary.json`
  - `outputs/workflow/04_event_preview.csv`
  - `outputs/tables/event_inclusion_summary.csv`
- Filled the Background & Methods Event Dataset tab with real preview and summary data.
- Run summary: 283 raw events, 283 included events, 0 excluded events, 93 tickers, 11 industries, and 0 duplicate event IDs.

## Text Features

- Added `configs/text_feature_config.json` from the validated V3 prototype config.
- Added `code/05_text_features.py`.
- Updated `run.py` with `--step text_features`.
- Built official event-level feature datasets:
  - `data/processed/event_level.csv`
  - `data/processed/event_level.parquet`
- Generated Step 5 workflow artifacts:
  - `outputs/workflow/05_text_features_summary.json`
  - `outputs/workflow/05_text_features_preview.csv`
  - `outputs/tables/topic_summary.csv`
  - `outputs/tables/topic_detail_summary.csv`
  - `outputs/tables/severity_bucket_summary.csv`
- Filled the Background & Methods Text Features tab with real preview and summary data.
- Run summary: 283 events, 6 grouped topics, 11 detail labels, 3 severity buckets, classifier `v3_grouped_rule_based_regex`.
- Topic counts matched the validated V3 prototype distribution: `accounting_financial_reporting` 132, `routine_other` 48, `mda_liquidity` 40, `revenue_non_gaap` 31, `business_structure` 22, and `risk_legal_regulatory` 10.

## Market Data

- Added `code/06_market_data.py`.
- Updated `run.py` with `--step market`.
- Copied the archived yfinance price cache into official `data/raw/market/`.
- Built official market datasets:
  - `data/processed/market_data.csv`
  - `data/processed/market_data.parquet`
- Generated Step 6 workflow artifacts:
  - `outputs/workflow/06_market_data_summary.json`
  - `outputs/workflow/06_market_data_preview.csv`
  - `outputs/tables/market_coverage_summary.csv`
- Filled the Background & Methods Market Data tab with real preview and coverage data.
- Run summary: 239,795 market rows, 94 tickers, 93 event tickers covered, benchmark `SPY`, date range `2015-09-09` to `2026-03-17`.

## Event Study and Regression

- Added `code/07_event_study_regression.py`.
- Updated `run.py` with `--step event_study`.
- Built official event-study and regression datasets:
  - `data/processed/event_time.csv`
  - `data/processed/event_time.parquet`
  - `data/processed/event_time_qa.csv`
  - `data/processed/regression_dataset.csv`
  - `data/processed/regression_dataset.parquet`
- Generated Step 7 workflow artifacts:
  - `outputs/workflow/07_event_study_regression_summary.json`
  - `outputs/workflow/07_event_time_preview.csv`
  - `outputs/workflow/07_regression_dataset_preview.csv`
- Generated final result artifacts:
  - `outputs/tables/average_car.csv`
  - `outputs/tables/topic_average_car.csv`
  - `outputs/tables/topic_car_summary.csv`
  - `outputs/tables/event_summary.csv`
  - `outputs/tables/baseline_regression.csv`
  - `outputs/tables/regression_CAR_-3_-1.csv`
  - `outputs/tables/regression_CAR_1_60.csv`
  - `outputs/figures/average_car_plot.png`
  - `outputs/figures/topic_average_car.png`
- Filled the Background & Methods Event Study & Regression tab with real preview and summary data.
- Updated Final Results to read official outputs and added topic-selectable CAR and regression-window selection.
- Run summary: 18,082 event-time rows, 283 matched events, 283 regression rows, 273 regression-included rows, and 6 topics.

## Final Results Polish V1

- Reworked `pages/02_Final_Results.py` into four tabs: Topic CAR, Regression Playground, Result Tables, and Static Figures.
- Added topic-selectable and relative-day-window-selectable CAR charts.
- Added a regression playground with fixed main variables `topic` and `severity_score`.
- Added playable controls for `industry` and `pre_event_volatility`, matching the approved regression-control scope.
- Added selectable outcomes for `CAR_-3_-1`, `CAR_-1_3`, and `CAR_1_60`.
- Kept underlying result tables and static figures available for audit.

## GitHub Submission Prep

- Added `README.md` with setup, run, and pipeline reproduction instructions.
- Updated `.gitignore` to exclude `Archive Prototype/` in addition to `.venv/`, `.envrc`, `AGENTS.md`, caches, and Python bytecode.
- Removed `Archive Prototype/` from Git tracking with `git rm --cached`, while keeping the local folder on disk.
- Updated `code/02_sec_raw_inventory.py` and `code/06_market_data.py` so clean clones use official `data/raw/sec/` and `data/raw/market/` caches when archive is absent.
- Verified the clean-cache path with `./.venv/bin/python run.py --step all --no-archive-copy`.

## Final Results Regression Summary Polish

- Updated `pages/02_Final_Results.py` so Regression Playground now shows a summary table across `CAR_-3_-1`, `CAR_-1_3`, and `CAR_1_60`.
- Kept `topic` and `severity_score` as fixed main variables and retained playable controls for `industry` and `pre_event_volatility`.
- Formatted key coefficients with HC1 robust standard errors and significance stars.
- Moved the full coefficient table into an expander under the selected detailed model.
- Verified syntax with `./.venv/bin/python -m py_compile pages/02_Final_Results.py`.

## Event-Study Volatility Window Polish

- Updated `configs/sample_100_10y.json` so event-time construction starts at relative day `-150`.
- Set `pre_event_volatility` to the stock-return standard deviation over relative trading days `[-150,-31]`.
- Updated `pages/01_Background_and_Methods.py` to define market-adjusted CAR and explain why `[-150,-31]` is used instead of a near-event or very long window.
- Updated `code/06_market_data.py` so the official market cache refreshes through yfinance when configured event/pre-event windows require more coverage.
- Refreshed market cache with `./.venv/bin/python run.py --step market --no-archive-copy`.
- Rebuilt event-study and regression outputs with `./.venv/bin/python run.py --step event_study`.
- Run summary after rebuild: 58,116 event-time rows, 283 matched events, 283 regression rows, 273 regression-included rows, and `pre_event_volatility_window = [-150,-31]`.

## Regression-Ready Dataset Display

- Added a visible `Regression-Ready Analysis Dataset` section to `pages/02_Final_Results.py`.
- The table shows the merged event-level evidence used by regressions: event identity, ticker, firm, event date, industry, topic, topic detail, classifier version, severity score, CAR windows, pre-event volatility, and inclusion status.
- Added filters for inclusion status, topic, industry, and ticker search.
- Verified syntax with `./.venv/bin/python -m py_compile pages/02_Final_Results.py`.

## Streamlit Navigation Cleanup

- Reworked `app.py` into an `Overview / Research Design` landing page focused on the research question, empirical design, main outputs, and current baseline.
- Removed redundant homepage sections: Current Build, Key Features, Navigation, and Repository Layout.
- Removed the standalone `pages/03_Research_Design.py` page after merging its useful content into the overview page.
- Simplified the Background & Methods overview tab so the empirical flow appears only on the overview/research-design page.

## S&P 500 Reference Filter Polish

- Replaced the large `st.multiselect` chip filters on the S&P 500 reference table with compact dropdown filters.
- The `GICS sub-industry` dropdown now depends on the selected `GICS sector`, while both controls keep an `All` option.
- Kept the full reference table directly visible and sortable through Streamlit's dataframe UI.

## SEC Raw Download Tab Cleanup

- Reworked the SEC Raw Download tab around `outputs/workflow/02_sec_thread_inventory.csv` as the core table.
- Replaced scattered metric cards, output-file lists, form summary blocks, and expanders with one compact KPI summary table.
- Added table filters for ticker, normalized form in thread, and company-submission availability.
- Added a path note pointing users to `data/raw/sec/` for original raw correspondence files and `outputs/workflow/02_sec_thread_inventory.csv` for the workflow inventory.

## Filing to Thread Tab Cleanup

- Reworked the Filing to Thread tab around `data/processed/thread_level.csv` / `.parquet` as the core table.
- Replaced metric cards, processing logic, output-file lists, and preview expanders with a compact summary table and direct thread-level dataset display.
- Kept `thread_role_summary.csv` visible to explain how raw filing rows are classified before grouping.
- Added filters for thread ticker, industry, and text status.
- Added notes explaining why the dataset moves from 100 raw tickers to 93 thread tickers, how `thread_id` is sourced from the V3 raw cache, and what `thread_duration_days` means.
- Removed duplicated staff-letter and filer-response counts from the compact summary and ordered the tab as Summary, Thread-Level Dataset, Filing Role Summary, then Notes.
- Added a short `thread_id` source note to SEC Raw Download and a fuller thread-membership grouping explanation to Filing to Thread Notes.
- Moved the Filing to Thread notes into an expander and clarified the metadata, chronology, form-pattern, accession/folder, and text-reference signals used to identify one SEC correspondence thread.
- Reorganized Filing to Thread notes into themed sections for ticker coverage, thread ID and membership, and thread duration.

## Event Dataset Tab Cleanup

- Reworked the Event Dataset tab around `data/processed/event_level_base.csv` / `.parquet` as the core table.
- Replaced metric cards, processing logic, output-file lists, preview table, and full-table expander with one compact event summary table and a direct event-level dataset display.
- Added filters for event ticker and event industry.
- Removed the inclusion summary table because all current events are included and the table adds little presentation value.
- Added notes explaining what changes from thread-level data to event-level data, how event dates are anchored, and why this table is the base for topic/severity and market/CAR steps.

## Text Features Tab Cleanup

- Reworked the Text Features tab around `data/processed/event_level.csv` / `.parquet` as the core table.
- Replaced metric cards, processing logic, output-file lists, preview table, and full-table expander with one compact text-feature summary table and a direct event-level dataset display.
- Added filters for topic, severity bucket, industry, and ticker search.
- Kept Topic Summary and Severity Buckets as distribution tables.
- Added separate explainers for the V3 grouped rule-based regex topic classifier and the severity scoring algorithm, including the meaning of `severity_bucket`.
- Expanded the severity scoring explainer with a multiline formula, `amendment_indicator` definition, current score range, and current low/medium/high bucket cutoffs.
- Expanded the topic classifier explainer with the predefined regex topic pattern focuses and the `topic_detail` to grouped `topic` mapping.

## Market Data Tab Cleanup

- Reworked the Market Data tab around `data/processed/market_data.csv` / `.parquet` as the core table.
- Replaced metric cards, processing logic, output-file lists, preview table, and full-table expander with one compact market summary table and a direct daily market panel display.
- Added filters for market ticker, ticker type, and date range.
- Moved Ticker Coverage into an expander as the coverage validation table.
- Added notes explaining cache/yfinance sourcing, the 93 event tickers plus SPY benchmark universe, and that abnormal returns/CAR are calculated in Step 7.

## Event Study and Regression Tab Cleanup

- Reworked the Event Study & Regression tab around two core tables: `data/processed/event_time.csv` / `.parquet` and `data/processed/regression_dataset.csv` / `.parquet`.
- Removed the summary metrics, output-file table, event-time QA table, topic CAR summary, average CAR table, topic-average CAR table, and baseline regression table from Background & Methods.
- Added filters for the event-time panel by ticker, event ID, and relative trading-day range.
- Added filters for the regression-ready dataset by topic, regression inclusion status, and ticker search.
- Added grouped notes explaining event-time construction, market-adjusted abnormal return and CAR definitions, pre-event volatility, regression dataset construction, and regression inclusion.

## Background and Methods Tab Pruning

- Removed the `Full Code Example` tab from Background & Methods so the presentation page ends with the seven data/process steps.

## Final Results Topic CAR Cleanup

- Removed the standalone `Static Figures` tab from Final Results.
- Moved the average CAR and topic-average CAR PNG snapshots into a collapsible `Static Figure Snapshots` section under the Topic CAR tab.
- Replaced the topic multiselect with a compact `All topics` / single-topic dropdown.
- Moved the three-window Topic CAR summary above the charts and displayed the CAR windows explicitly.
- Replaced Streamlit default line charts with Altair charts that include a relative-day-0 event line, a zero-CAR reference line, and richer tooltips.
- Moved Topic CAR Summary above the filters, stacked the All Events and By Topic charts vertically, and removed the underlying topic CAR rows expander.
- Renamed Topic CAR Summary display columns to the consistent CAR window names: `CAR_-3_-1`, `CAR_-1_3`, and `CAR_1_60`.
- Added shaded event-window bands to the Topic CAR charts for `CAR_-3_-1`, `CAR_-1_3`, and `CAR_1_60`.
- Added point layers to the Topic CAR charts so hover tooltips are easier to trigger.
- Added a `CAR Window Values by Topic` table below the charts to show aggregate CAR values for the shaded windows.
- Reverted the point-layer hover change and `CAR Window Values by Topic` table at user request, returning Topic CAR to the prior shaded-window chart layout.
- Added a `CAR window shading` multiselect so users can choose which shaded CAR windows appear on the Topic CAR charts.
- Changed `CAR window shading` from multiselect to a compact dropdown matching the Topic filter style.
- Renamed Topic CAR Summary window columns to `mean_CAR_-3_-1`, `mean_CAR_-1_3`, and `mean_CAR_1_60` to make clear that they are topic-level means.
- Formatted Topic CAR Summary CAR-window columns as percentages with two decimals.
- Updated Topic CAR chart display so cumulative CAR resets at the selected window start by cumulatively summing average abnormal returns.
- Added a `Topic CAR Code Logic` expander above static figure snapshots.
- Updated Step 7 output generation so `average_car.csv` and `topic_average_car.csv` compute `average_CAR` from cumulative average abnormal returns, then regenerated the event-study outputs and static figures.
